<p style="color: green"><%= notice %></p>

<% content_for :title, "Comments" %>

<h1>Comments</h1>

<div id="comments">
  <% @comments.each do |comment| %>
    <hr>
    <%= render comment %>
    <p>
      <% if can? :manage, comment %>
        <%= link_to "Show this comment",article_comment_path(comment.article, comment) %>
      <%end%>
    </p>
  <% end %>
</div>

<% if can? :create, Comment %>
  <%= link_to "New comment", new_article_comment_path() %>
<% end %>

<%= link_to "Back to Article",article_path(@article) %>