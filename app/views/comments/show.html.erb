<p style="color: green"><%= notice %></p>

<%= render @comment %>

<div>
  <% if can? :edit, Comment %>
    <%= link_to "Edit this comment", edit_article_comment_path(@comment.article, @comment) %> |
    <%= button_to "Destroy this comment", article_comment_path(@comment.article, @comment), method: :delete %>
  <% end %>
  
  <%= link_to "Voltar para o artigo", article_path(@article) %>

</div>
